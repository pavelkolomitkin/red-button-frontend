import { PageHeaderDirective } from './page-header.directive';

describe('PageHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new PageHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
