import { MultiLinePipe } from './multi-line.pipe';

describe('MultiLinePipe', () => {
  it('create an instance', () => {
    const pipe = new MultiLinePipe();
    expect(pipe).toBeTruthy();
  });
});
