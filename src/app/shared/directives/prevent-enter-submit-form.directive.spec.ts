import { PreventEnterSubmitFormDirective } from './prevent-enter-submit-form.directive';

describe('PreventEnterSubmitFormDirective', () => {
  it('should create an instance', () => {
    const directive = new PreventEnterSubmitFormDirective();
    expect(directive).toBeTruthy();
  });
});
